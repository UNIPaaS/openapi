{
  "name": "openapi",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical, versioned OpenAPI spec for the UNIPaaS Platform API.",
  "scripts": {
    "test": "tsx --test scripts/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint:spec": "spectral lint openapi.json --ruleset .spectral.yaml",
    "publish:spec": "tsx scripts/publish.ts"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.14.2",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
